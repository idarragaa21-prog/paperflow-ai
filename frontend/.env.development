VITE_DEMO_MODE=false
# Leave VITE_API_BASE_URL empty in development so all requests go through
# Vite's proxy on http://localhost:5173. Keeps cookies on the same origin
# and avoids SameSite=Lax cross-site blocking on login.
VITE_API_BASE_URL=
