<<<<<<< SEARCH
  it('blocks password change when confirmation does not match', async () => {
    render(<SettingsPage />);

    const inputs = screen.getAllByDisplayValue('');
    await userEvent.type(inputs[0], 'current-password');
    await userEvent.type(inputs[1], 'new-password');
    await userEvent.type(inputs[2], 'different-password');
    fireEvent.click(screen.getByRole('button', { name: 'Update password' }));

    await waitFor(() => {
      expect(screen.getByText('Passwords do not match.')).toBeInTheDocument();
    });
  });
=======
  it('blocks password change when confirmation does not match', async () => {
    render(<SettingsPage />);

    const passwordInputs = screen.getAllByRole('textbox').filter(input => (input as HTMLInputElement).type === 'password') as HTMLInputElement[];
    if (passwordInputs.length === 0) {
      // Fallback: If role="textbox" doesn't catch type="password" (jsdom limitation sometimes), use querySelectorAll
      const allPasswordInputs = document.querySelectorAll('input[type="password"]');
      await userEvent.type(allPasswordInputs[0], 'current-password');
      await userEvent.type(allPasswordInputs[1], 'new-password');
      await userEvent.type(allPasswordInputs[2], 'different-password');
    } else {
      await userEvent.type(passwordInputs[0], 'current-password');
      await userEvent.type(passwordInputs[1], 'new-password');
      await userEvent.type(passwordInputs[2], 'different-password');
    }

    fireEvent.click(screen.getByRole('button', { name: 'Update password' }));

    await waitFor(() => {
      expect(screen.getByText('Passwords do not match.')).toBeInTheDocument();
    });
  });
>>>>>>> REPLACE
