# Runtime dependencies only (all ship macOS/Linux wheels — no system libraries).
# For tests:  pip install pytest httpx
# For sharper Word figures (needs system cairo):  pip install cairosvg
fastapi>=0.109
uvicorn[standard]>=0.27
pydantic>=2.5
numpy>=1.26
scipy>=1.11
python-docx>=1.1
openpyxl>=3.1
# Figures (SVG→PNG for Word export) are rasterised with PyMuPDF — wheels on every
# platform incl. Apple Silicon + Python 3.14, no system libraries. (svglib/reportlab
# are NOT required: svglib 2.x needs reportlab≥4.4 which needs system cairo, and
# reportlab 3.x has no recent wheels and fails to build from source.)
pymupdf>=1.24
python-multipart>=0.0.9
